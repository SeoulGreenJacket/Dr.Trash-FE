import React from 'react';
import {AlertBox, Title, TitleBox} from '../../../styles/main/home/alertBox';

const Alert = () => {
  return (
    <AlertBox>
      <TitleBox>
        <Title>의견을 나눠보세요.</Title>
      </TitleBox>
    </AlertBox>
  );
};

export default Alert;
